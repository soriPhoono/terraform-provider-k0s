# Examples

This directory contains example Terraform configurations for the k0s provider.

- `provider/` - Provider configuration example.
- `resources/k0s_cluster/` - Example `k0s_cluster` resource.
